<h1>Links Page</h1>
  <ul>
    <% @links.each do |link| %>
      <li><%= link_to link.url, link_path(link)%></li>
    <%end%>
  </ul>

<%= link_to "New link", new_link_path, class: "btn btn-default" %>
