
<h1>Users Page</h1>
  <ul>
    <% @users.each do |usr| %>
      <li><%= link_to usr.email, user_path(usr)%></li>
    <%end%>
  </ul>

<%= link_to "New User", new_user_path, class: "btn btn-default" %>
