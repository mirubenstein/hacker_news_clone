<h1>Comments Page</h1>
  <ul>
    <% @comments.each do |comment| %>
      <li><%= link_to comment.description, comment_path(comment)%></li>
    <%end%>
  </ul>

<%= link_to "New comment", new_comment_path, class: "btn btn-default" %>
